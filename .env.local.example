ANTHROPIC_API_KEY=
TAVILY_API_KEY=
EXA_API_KEY=

# Vercel Blob token. Needed only to read/write briefings to Blob (production
# and the weekly GitHub Actions run). Leave BLANK for local dev — briefings
# then read/write data/briefings/ on local disk instead.
BLOB_READ_WRITE_TOKEN=
